{
  "name": "pixgate-gui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "PixGate widget-setup GUI — a Vite+Svelte SPA hosted on GitHub Pages that configures a PixGate device over its JSON API.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.1.0",
    "vite": "^5.4.0"
  }
}
